<div class="page-header">
  <h2 class="text-info">
    Upcoming Kellogg Happenings
    <a href="/events/new" class="btn btn-md btn-info">New Event</a>
  </h2>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>The Happening</th>
        <th>Start Time</th>
        <th>End Time</th>
        <th>Face Value</th>
        <th>Tickets Available</th>
        <th>CampusGroups</th>
        <th>Type</th>

        <th>Actions</th>
      </tr>

      <% @events.each do |event| %>
      <tr>
        <td>
          <%= event.name %> </br>
          <img src="<%= event.image_url %>" width="80">
        </td>
        <td>
          <%= event.start_time.strftime('%b %e, %Y') %> </br>
          <%= event.start_time.strftime('%l:%M%P') %>
        </td>
        <td>
          <%= event.end_time.strftime('%b %e, %Y') %> </br>
          <%= event.end_time.strftime('%l:%M%P') %>
        </td>
        <td><%= number_to_currency(event.ticket_face_value) %></td>
        <td><%= event.tickets.sum(:quantity) %></td>
        <td><a href="<%= event.campusgroups_url %>"> Click Here </a></td>

        <td><%= event.event_category %></td>
        <td>
          <a href="/events/<%= event.id %>" class="btn btn-sm btn-primary">Show</a>
          <a href="/events/<%= event.id %>/edit" class="btn btn-sm btn-warning">Edit</a>
          <a href="/delete_event/<%= event.id %>" class="btn btn-sm btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
